# AR Lookup

Draft UI for image AR analysis and AI chat.

## Run the frontend

```bash
cd frontend
npm install
npm run dev
```

## Folders

- `frontend` — React app (Vite)
- `backend` — reserved for APIs later
